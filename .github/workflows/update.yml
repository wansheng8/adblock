name: 更新广告过滤规则

on:
  schedule:
    # 每天 UTC 时间 08:00 运行（对应上海时间 16:00）
    - cron: '0 8 * * *'
  workflow_dispatch:  # 允许手动触发
  push:
    branches:
      - main
    paths:
      - 'rules/sources/**'
      - 'run.py'
      - '.github/workflows/update.yml'

env:
  PYTHONUNBUFFERED: 1
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  update-rules:
    runs-on: ubuntu-latest
    
    # 增加资源限制
    timeout-minutes: 30
    
    steps:
    - name: 检查代码
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: 设置Python环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: 清理旧文件
      run: |
        rm -rf rules/outputs/*
        rm -rf temp/
        mkdir -p rules/outputs temp
        echo "已清理旧文件"
        
    - name: 运行规则收集器
      run: |
        python run.py
        
    - name: 配置Git
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
    - name: 拉取远程更新并合并
      run: |
        # 拉取最新的远程更改
        git fetch origin main
        # 尝试合并，如果有冲突则使用ours策略保留我们的更改
        git merge --strategy-option=ours origin/main || echo "合并冲突，使用我们的版本"
        
    - name: 检查文件变化
      id: check-changes
      run: |
        git add -A
        
        # 检查是否有变化
        if git diff --cached --quiet; then
          echo "没有变化需要提交"
          echo "has_changes=false" >> $GITHUB_OUTPUT
        else
          echo "检测到变化，将提交更新"
          echo "has_changes=true" >> $GITHUB_OUTPUT
          UPDATE_TIME=$(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S')
          echo "update_time=$UPDATE_TIME" >> $GITHUB_OUTPUT
          echo "更新时间: $UPDATE_TIME"
        fi
        
    - name: 提交并推送更新
      if: steps.check-changes.outputs.has_changes == 'true'
      run: |
        # 提交更改
        git commit -m "🔧 自动更新规则文件 [${{ steps.check-changes.outputs.update_time }}]"
        
        # 强制推送（因为我们已合并了远程更改）
        git push --force-with-lease
        
    - name: 更新完成通知
      run: |
        echo "✅ 广告过滤规则更新完成！"
        echo "📅 更新时间: $(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S')"
        echo "📊 输出目录: rules/outputs/"
        
    - name: 生成摘要报告
      if: always()
      run: |
        echo "## 📊 规则更新报告" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ -f "rules/outputs/stats.json" ]; then
          echo "✅ 更新任务已完成！" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # 读取统计信息
          total_rules=$(grep -o '"total_rules": [0-9]*' rules/outputs/stats.json | grep -o '[0-9]*' || echo "0")
          white_rules=$(grep -o '"white_rules": [0-9]*' rules/outputs/stats.json | grep -o '[0-9]*' || echo "0")
          black_rules=$(grep -o '"black_rules": [0-9]*' rules/outputs/stats.json | grep -o '[0-9]*' || echo "0")
          sources_processed=$(grep -o '"sources_processed": [0-9]*' rules/outputs/stats.json | grep -o '[0-9]*' || echo "0")
          sources_failed=$(grep -o '"sources_failed": [0-9]*' rules/outputs/stats.json | grep -o '[0-9]*' || echo "0")
          
          echo "**统计信息:**" >> $GITHUB_STEP_SUMMARY
          echo "- 总规则数: $total_rules 条" >> $GITHUB_STEP_SUMMARY
          echo "- 白名单规则: $white_rules 条" >> $GITHUB_STEP_SUMMARY
          echo "- 黑名单规则: $black_rules 条" >> $GITHUB_STEP_SUMMARY
          echo "- 成功源: $sources_processed 个" >> $GITHUB_STEP_SUMMARY
          echo "- 失败源: $sources_failed 个" >> $GITHUB_STEP_SUMMARY
        else
          echo "⚠️ 更新任务可能未完全成功" >> $GITHUB_STEP_SUMMARY
          echo "请检查运行日志" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**更新时间 (上海时间):** $(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        echo "### 🔗 订阅链接" >> $GITHUB_STEP_SUMMARY
        echo "- 混合规则: https://raw.githubusercontent.com/${{ github.repository }}/main/rules/outputs/adblock.txt" >> $GITHUB_STEP_SUMMARY
        echo "- 压缩版本: https://raw.githubusercontent.com/${{ github.repository }}/main/rules/outputs/adblock.txt.gz" >> $GITHUB_STEP_SUMMARY
        echo "- 仅黑名单: https://raw.githubusercontent.com/${{ github.repository }}/main/rules/outputs/black_only.txt" >> $GITHUB_STEP_SUMMARY
        echo "- 仅白名单: https://raw.githubusercontent.com/${{ github.repository }}/main/rules/outputs/white_only.txt" >> $GITHUB_STEP_SUMMARY
        
        if [ -f "rules/outputs/stats.json" ]; then
          echo "- 统计文件: https://raw.githubusercontent.com/${{ github.repository }}/main/rules/outputs/stats.json" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📁 文件列表" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        ls -la rules/outputs/ >> $GITHUB_STEP_SUMMARY 2>/dev/null || echo "输出目录为空" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
